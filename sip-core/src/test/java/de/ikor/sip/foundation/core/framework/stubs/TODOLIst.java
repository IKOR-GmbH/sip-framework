package de.ikor.sip.foundation.core.framework.stubs;

public class TODOLIst {
  // TODO When .build() is missing on CentralRouter config --> nasty, understandable exception
  // TODO handle this kind of situation with SIP? When route is split into two or more and requires
  // ie.  .from(direct:..)
}
